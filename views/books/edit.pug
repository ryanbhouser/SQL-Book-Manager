extends ../layout

block content
  .container
    include ../errors

  header.header
    .container
      h1= title
      a.button.btn-secondary(href='/books') Cancel
  section.content
    include ../errors
    .container
      form(action="/books/" + books.id +"/edit", method="post")
        include form