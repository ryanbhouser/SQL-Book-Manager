extends ../layout

block content
  header.header
    .container
      h1= title
      div
        a.button.btn-secondary(href='/') Cancel
  section.content
    .container
      form(action="/books/new", method="post")
        include form
    .container
      include ../errors